# Luxury Intelligence — Improvements Backlog

> **LIVE DOCUMENT — Keep this up to date.**
>
> This file is a running backlog of planned improvements, GEO/SEO fixes, and product ideas
> for luxury-intel.com. It is read by AI assistants (Claude Code, etc.) when they explore
> this repository — treat every entry as current unless explicitly marked `[DONE]` or `[DROPPED]`.
>
> **When you complete an item**, mark it `[DONE]` and add a one-line note on where/how it was implemented.
> **When you add a new idea**, add it under the relevant section with a brief rationale.
> **Review and prune** stale or superseded items when you revisit this file.

---

## GEO / SEO

### High Priority

- [ ] **Add named editor / Person schema** — Add "Alexey Kochemirovskiy" as named editor
  on the About page with a bio (role at Pandora, ex-consultant, ex-scientist, Copenhagen),
  update root layout JSON-LD to include Person entity with `sameAs` LinkedIn URL,
  update `author` in digest page Article schema from Organization-only to Person.
  *Rationale: AI engines anchor citations to verifiable human expertise. "AK" alone is insufficient.*

- [ ] **Create evergreen topic hub pages** — Build persistent `/topics/ai-in-ecommerce`,
  `/topics/luxury-retail-trends`, `/topics/jewellery-industry-news` pages that aggregate
  and synthesise across digest issues.
  *Rationale: AI systems (Perplexity, Google AI Overviews) cite topic-specific, persistent pages,
  not temporal weekly digest archives. These pages are the primary GEO citability lever.*

- [ ] **Editor's Take section** — AI-generated, first-person editorial opinion (150-200 words)
  on the week's most interesting cross-category signal. Support `editorialTake` field in digest JSON
  (auto-generated by pipeline) + `editorialTakeOverride: true` flag to protect manual edits.
  Prompt: reaction not summary, picks ONE thread across categories, ends with a provocation.
  *Rationale: Original synthesis is what AI engines prefer to cite over aggregated summaries.*

### Medium Priority

- [ ] **hreflang on remaining pages** — Add `alternates.languages` to `/archive`,
  `/subscribe`, `/es/archive`, `/da/archive`, `/es/subscribe`, `/da/subscribe`,
  `/es/methodology`, `/da/methodology`. Currently only `/`, `/about`, `/methodology`
  have been updated.

- [ ] **Organisation schema — `sameAs` links** — Add LinkedIn company page URL and any
  other directory profiles (Crunchbase, etc.) to the `sameAs` array in the WebSite
  schema in `app/layout.tsx`.
  *Rationale: Helps AI engines anchor the "Luxury Intelligence" entity to known external records.*

- [ ] **Speakable schema** — Add `Speakable` JSON-LD to digest pages pointing to the
  `oneSentenceSummary` and `weeklyInsight` fields. Relevant for voice/audio AI interfaces
  given the site already has a podcast.

### Backlog / Ideas

- [ ] **Knowledge Graph entity** — Create a LinkedIn company page for "Luxury Intelligence",
  consider Crunchbase/Wikidata entry. External entity anchors improve AI citation reliability.

- [ ] **FAQ sections for locale pages** — Add localised FAQ (ES/DA) to `/es/about` and
  `/da/about` pages mirroring the English FAQ added to `/about`.

- [ ] **Topical archive pages** — Beyond evergreen hubs, consider `/archive/ai-strategy`,
  `/archive/jewellery` that filter the existing digest archive by category.

---

## Content & Editorial

- [ ] **Original data / statistics** — Add at least one original data point per digest
  (e.g. "763 articles ingested this week, up 12% from last week") that AI engines can
  cite as primary source data.

- [ ] **Source attribution improvement** — Each digest article card shows source favicon
  and domain. Consider adding the publication date more prominently and linking to the
  source's homepage (not just the article) to strengthen source-level E-E-A-T signals.

---

## Technical / Infrastructure

- [ ] **Remaining sitemap static pages lastmod** — Static pages (`/about`, `/methodology`,
  etc.) currently use `now` (build time) as lastmod. Consider using known content-change
  dates or a file-based changelog to give accurate per-page freshness signals.

- [ ] **Archive page canonical + hreflang** — `/archive` page lacks explicit canonical
  and hreflang. Add `getSiteUrl()` and `alternates` metadata.

---

## Product / Features

- [ ] **Competitor Watch** — Already exists at `/competitor-watch`. Evaluate GEO value
  of making this publicly indexable vs. subscriber-gated.

- [ ] **Search page SEO** — `/search` is in the sitemap but likely renders client-side.
  Ensure it has a meaningful server-rendered title and meta description.

---

*Last reviewed: 2026-04-03*
